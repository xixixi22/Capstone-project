This is Rui Liu's master Capstone project.

In "util folder", it is data preprocessing code. In "baselineModel.py" file, it is the main implementation code. "Data folder" contains the data which i used.
